[package]
name = "guyda-portfolio"
version = "0.2.0"
edition = "2021"
description = "Guy Dahan — live resume"
license = "MIT"

[dependencies]
console_error_panic_hook = "0.1"
leptos = { version = "0.8.10", features = ["csr"] }
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = [
    "Document",
    "Element",
    "HtmlCanvasElement",
    "MediaQueryList",
    "Storage",
    "Window",
] }
